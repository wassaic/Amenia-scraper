services:
  - type: web
    name: amenia-scraper-api
    env: node
    region: oregon
    plan: free
    buildCommand: |
      npm install
      npx puppeteer browsers install chrome@stable
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
